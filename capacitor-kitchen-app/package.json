{
  "name": "kitchen-app-shell",
  "version": "1.0.0",
  "private": true,
  "description": "עטיפת Android (Capacitor) ל-admin.html של אתר אדלה בשוק — פותרת הדפסת USB תרמית בלי root. פרויקט-תבנית: ניתן לעטוף בו כל דף HTML קיים.",
  "scripts": {
    "build": "node scripts/sync-web.js",
    "android:sync": "npm run build && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.4",
    "@capacitor/core": "^7.4.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.4",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
